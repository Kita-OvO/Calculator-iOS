// Operation.swift
// Enum defining the arithmetic operations supported by the calculator

import Foundation

enum Operation {
    case add      // Addition
    case subtract // Subtraction
    case multiply // Multiplication
    case divide   // Division
    case none     // No operator selected (initial state)
}
